<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="teamproj.mybatis.mapper.teamproj2">
<select id="findAll2" parameterType="Map" resultType="Cal2Dto">
		SELECT * FROM CAL2 ORDER BY no DESC
</select>
<insert id="calInsert2" parameterType="Map">
	<selectKey keyProperty="calno2" resultType="int" order="BEFORE">
		SELECT SEQ_CAL2NO.NEXTVAL FROM DUAL
	</selectKey>
		INSERT INTO CAL2 VALUES(SEQ_CAL2NO.NEXTVAL,#{title},#{startdate},#{enddate},#{color},'park')
</insert>
<select id="FindRecordByNo" parameterType="Map" resultType="Cal2Dto">
	SELECT title=#{title},startdate=#{startdate} FROM cal2 WHERE no=#{no}
</select>




</mapper>