<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="teamproj.mybatis.mapper.teamproj">
	<!-- 여기다가 sql문 작성 하면 됨 -->
<select id="findAll" parameterType="Map" resultType="CalDto">
		SELECT * FROM CAL ORDER BY no DESC
</select>
<insert id="calInsert" parameterType="Map">
	<selectKey keyProperty="calno" resultType="int" order="BEFORE">
		SELECT SEQ_CALNO.NEXTVAL FROM DUAL
	</selectKey>
		INSERT INTO CAL VALUES(SEQ_CALNO.NEXTVAL,#{title},#{startdate},#{enddate},'yellow','KIM')
</insert>



</mapper>