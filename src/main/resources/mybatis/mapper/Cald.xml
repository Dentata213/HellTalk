<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.Cald">
<select id="findAll" parameterType="Map" resultType="CaldDto">
		SELECT * FROM CALDAILY ORDER BY CALD_NO DESC
</select>
 
<insert id="caldInset" parameterType="Map">
	<selectKey keyProperty="caldno" resultType="int" order="BEFORE">
		SELECT SEQ_CALDNO.NEXTVAL FROM DUAL
	</selectKey>
		INSERT INTO CALDAILY VALUES(SEQ_CALDNO.NEXTVAL,'가상의회원',#{caldtitle},#{caldcontent},#{caldstartDate},#{caldcolor})
</insert>
<!-- 
<select id="FindRecordByNo" parameterType="Map" resultType="Cal2Dto">
	SELECT title=#{title},startdate=#{startdate} FROM cal2 WHERE no=#{no}
	
</select>
-->




</mapper>